#include "hash_tableInterface.h"
#include "hash_tableTypes.h"

#include <stdio.h>
/*
#define TableSize 100 //The size of the table


struct OAInfoType
{
	int first;
    int second;   // two dummy information fields 
} OAInfoType;

struct OAHTableEntry
{
	KeyType key;
   struct InfoType info;
} OAHTableEntry;

typedef OAHTableEntry HTable[TableSize];



HTCreate() //creates an empty hash
{

}

HTSize(hash) //returns the number of elements in the hash
{

}

HTGet(hash, key, pitem) //searches for key in the hash. If found, returns true and copiesthe item in the pitem pointer passed as argument
{

}

HTInsert(hash, key, item) //inserts a key (with its item) in the hash (replace if exists)
{

}

HTRemove(hash, key); //removes the key from the hash
{

}

HTVisit(hash, visit); //calls visit(hash, key, item) for all entries in the hash (in arbitraryorder). visit is a function given by the user
{

}

HTDestroy(hash); //destroys the hash by freeing all reserved memory
{

}
*/
